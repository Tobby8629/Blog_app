 <%@users.each do |each| %>
   <div class= "user">
    <div class = "image">
      <%= image_tag each.photo, alt: "user #{each.id} image" %>
    </div>
    <div class= "text">
      <h3><%= each.name %></h3>
      <h4>Number of posts: <%= each.posts_counter %></h4>
    </div>
  </div>
<%end%>
